# Environment configuration for production
# This file is loaded when running the API in production mode

# Server configuration
PORT=8000
HOST=0.0.0.0  # Listen on all available network interfaces

# CORS configuration
# Replace with your actual production frontend URL
CORS_ORIGINS=https://heart-ai.example.com

# Debug mode (disabled in production for security)
DEBUG=False